# Autonoe Planning Notes

## Session: 2026-02-21 (EXT-001 completion)

### Accomplished This Session

Implemented the full Bookwalker Discount Badge Chrome extension (EXT-001):

- `manifest.json` — Manifest V3, content script targeting `*://*.bookwalker.com.tw/*`, runs at `document_idle`
- `content.js` — Complete content script:
  - `isWishlistPage()` — guards against non-wishlist pages by checking URL pattern and/or presence of `div[id^="bw_buy_"]` containers
  - `extractOriginalPrice()` — reads `span.txt_del` for 原價:{N}元
  - `extractSpecialPrice()` — walks text nodes looking for 特價:{N}元
  - `calculateDiscount()` — `Math.round((1 - special/original) * 100)`
  - `badgeColor()` — green (#4CAF50) 1-10%, orange (#FF9800) 11-30%, red (#F44336) 31%+
  - `createBadge()` — inline-styled span with rounded corners, bold, padding, white text
  - `processContainer()` — deduplication guard, badge inserted after bw_buy_ div inside .wishH
- `test.js` — 25 JSDOM unit tests (all pass)
- `.gitignore` — excludes node_modules

### Acceptance Criteria Verified

All 11 acceptance criteria verified via automated tests (25/25 pass):

- [x] AC1: manifest.json MV3 with correct match pattern
- [x] AC2: Finds all bw_buy_* containers
- [x] AC3: Extracts original price from span.txt_del
- [x] AC4: Extracts special price from text node
- [x] AC5: Discount formula matches spec exactly
- [x] AC6: Badge element inserted after price div within .wishH
- [x] AC7: Color coding correct at all boundaries (10%, 11%, 30%, 31%)
- [x] AC8: Badge styled with border-radius, bold, padding
- [x] AC9: No badge for 0%, only-original, only-special, unparseable prices
- [x] AC10: Self-check guards against non-wishlist pages
- [x] AC11: No errors on silent-skip paths

### Issues Found and Fixed

None — implementation was correct on first pass. Test failures were only due to incorrect test helper (missing 原價: prefix in HTML) and JSDOM normalizing hex→rgb() in cssText assertions; both fixed in test.js.

### Current Project Status

- EXT-001: COMPLETE — committed as `feat: add core discount badge extension with color coding`
- EXT-002: PENDING — MutationObserver for dynamically loaded entries (blocked by EXT-001, now unblocked)
- All 25 unit tests passing
- No console errors in any test scenario
